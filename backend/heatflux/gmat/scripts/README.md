# GMAT Execution Note

No `GMAT` executable was detected in the current Linux environment. The file
`catch02_heatflux.script` is a skeleton showing the intended propagation setup.

When GMAT is installed, configure the exact RAAN/local-time values and report
ECI position and velocity at 30 s cadence. The exported data can then be used by
the Python heat-flux postprocessor.
